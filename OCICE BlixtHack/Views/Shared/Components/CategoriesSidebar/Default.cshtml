@model CategoriesVM
 <div class="sidebar-menu">
     <ul>
    @foreach (var category in Model.Categories)
        {
            <li class="list-group-item"><a asp-action="Category" asp-controller="Home" asp-route-categoryId="@category.Id">@category.Title</a></li>
               }
    </ul>
    </div>
